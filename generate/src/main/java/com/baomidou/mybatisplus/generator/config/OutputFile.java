package com.baomidou.mybatisplus.generator.config;

public enum OutputFile {
    entity,
    service,
    serviceImpl,
    mapper,
    mapperXml,
    controller,
    other,
    feign;

    private OutputFile() {
    }
}
