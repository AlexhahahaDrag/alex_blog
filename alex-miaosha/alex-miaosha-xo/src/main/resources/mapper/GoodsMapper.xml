<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alex.blog.miaosha.xo.mapper.GoodsMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.alex.blog.miaosha.common.entity.Goods">
        <result column="id" property="id" />
        <result column="goods_name" property="goodsName" />
        <result column="goods_title" property="goodsTitle" />
        <result column="goods_img" property="goodsImg" />
        <result column="goods_detail" property="goodsDetail" />
        <result column="goods_price" property="goodsPrice" />
        <result column="goods_stock" property="goodsStock" />
    </resultMap>
    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,
        goods_name, goods_title, goods_img, goods_detail, goods_price, goods_stock
    </sql>

    <sql id="vo_list">
        miaosha_goods.id as id, goods_name, goods_title, goods_img, goods_detail,
        goods_price, goods_stock, miaosha_price, stock_count, start_date, end_date
    </sql>

    <select id="getGoodsVoByGoodsId" resultType="com.alex.blog.miaosha.common.vo.GoodsVo">
        select <include refid="vo_list"></include>
        from miaosha_goods left join goods on miaosha_goods.goods_id = goods.id
        where goods_id = #{goodsId}
    </select>
</mapper>
